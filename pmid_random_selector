from Bio import Entrez
import random

Entrez.email = "osborn.s.xs07@gmail.com"

term = "Prostate cancer"

handle = Entrez.esearch(db="pubmed", term=term, retmax=1000)
record = Entrez.read(handle)
handle.close()

pmids = record["IdList"]

sample = random.sample(pmids, 10)
print(sample)